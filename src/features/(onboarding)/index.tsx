"use client";
import type React from "react";
import "@/styles/globals.scss";

export default function OnboardingWrapper({ children }: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
